import { process, services } from '@/lib/portfolio-data'

export function About() {
  return <section id="about" className="about-section section-pad"><div className="container"><div className="about-grid"><div><p className="eyebrow">A little about me</p><h2>Good design makes an idea feel <em>inevitable.</em></h2></div><div className="about-copy"><p>I&apos;m Yoshita, a graphic designer interested in the space where clear thinking meets expressive visuals. I like work that is useful, memorable, and a little unexpected.</p><p>From a single poster to a full campaign system, I bring structure to the process and care to the details.</p></div></div><div id="services" className="services-block"><div><p className="eyebrow">What I can help with</p><h3>Services</h3></div><div className="service-list">{services.map((service) => <div className="service-item" key={service.label}><strong>{service.label}</strong><span>{service.detail}</span></div>)}</div></div><div className="process-block"><div><p className="eyebrow">The approach</p><h3>How I work</h3></div><div className="process-list">{process.map(([number, title, detail]) => <div className="process-item" key={number}><span>{number}</span><div><strong>{title}</strong><p>{detail}</p></div></div>)}</div></div></div></section>
}
